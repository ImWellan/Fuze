<Window x:Class="FusePlayer.MediaInfoDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Informations sur le média"
        Width="610" Height="520" MinWidth="520" MinHeight="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Cursor="Arrow"
        ForceCursor="False"
        FontFamily="Segoe UI Variable Text">
    <Window.Resources>
        <Style x:Key="DialogScrollBarRepeatButton" TargetType="RepeatButton">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border x:Name="RepeatButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Stretch"
                                              VerticalAlignment="Stretch"
                                              ContentSource="Content" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RepeatButtonBorder" Property="Background" Value="#FF2B3038" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="RepeatButtonBorder" Property="Background" Value="#FF343B45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ScrollBar" BasedOn="{StaticResource FuzeScrollBarContextMenuHostStyle}">
            <Setter Property="Width" Value="13" />
            <Setter Property="Background" Value="#FF14171B" />
            <Setter Property="Foreground" Value="#FF60656D" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Width="13" Background="{TemplateBinding Background}"
                              SnapsToDevicePixels="True">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="15" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="15" />
                            </Grid.RowDefinitions>
                            <RepeatButton Grid.Row="0"
                                          Command="{x:Static ScrollBar.LineUpCommand}"
                                          CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                          HorizontalContentAlignment="Center"
                                          VerticalContentAlignment="Center"
                                          Background="#FF20242A" BorderThickness="0" Focusable="False"
                                          Style="{StaticResource DialogScrollBarRepeatButton}">
                                <Path Width="5" Height="3"
                                      HorizontalAlignment="Center" VerticalAlignment="Center"
                                      Data="M 0,3 L 2.5,0.5 L 5,3"
                                      Fill="Transparent" Stroke="#FF858A92" StrokeThickness="1"
                                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                      StrokeLineJoin="Round" Stretch="None" />
                            </RepeatButton>
                            <Track x:Name="PART_Track" Grid.Row="1"
                                   Width="13"
                                   HorizontalAlignment="Center"
                                   IsDirectionReversed="True"
                                   ClipToBounds="False">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}"
                                                  Background="Transparent" BorderThickness="0" Focusable="False"
                                                  Style="{StaticResource DialogScrollBarRepeatButton}" />
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Width="7" MinHeight="28" Margin="0,1"
                                           HorizontalAlignment="Center" ClipToBounds="False">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border x:Name="ScrollThumb"
                                                        Background="#FF555A62"
                                                        CornerRadius="3.5" />
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="ScrollThumb" Property="Background" Value="#FF6B7079" />
                                                    </Trigger>
                                                    <Trigger Property="IsDragging" Value="True">
                                                        <Setter TargetName="ScrollThumb" Property="Background" Value="#FF7A8089" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}"
                                                  Background="Transparent" BorderThickness="0" Focusable="False"
                                                  Style="{StaticResource DialogScrollBarRepeatButton}" />
                                </Track.IncreaseRepeatButton>
                            </Track>
                            <RepeatButton Grid.Row="2"
                                          Command="{x:Static ScrollBar.LineDownCommand}"
                                          CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                          HorizontalContentAlignment="Center"
                                          VerticalContentAlignment="Center"
                                          Background="#FF20242A" BorderThickness="0" Focusable="False"
                                          Style="{StaticResource DialogScrollBarRepeatButton}">
                                <Path Width="5" Height="3"
                                      HorizontalAlignment="Center" VerticalAlignment="Center"
                                      Data="M 0,0 L 2.5,2.5 L 5,0"
                                      Fill="Transparent" Stroke="#FF858A92" StrokeThickness="1"
                                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                      StrokeLineJoin="Round" Stretch="None" />
                            </RepeatButton>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MediaInfoSectionExpander" TargetType="Expander">
            <Setter Property="Margin" Value="0,0,0,8" />
            <Setter Property="Foreground" Value="#FFF3F4F6" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Expander">
                        <Border Background="#FF171A1F"
                                BorderBrush="#FF343A43"
                                BorderThickness="1"
                                CornerRadius="5">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <ToggleButton x:Name="HeaderButton"
                                              IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                              Content="{TemplateBinding Header}"
                                              Foreground="{TemplateBinding Foreground}"
                                              Cursor="Arrow"
                                              Focusable="False">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border x:Name="HeaderBackground"
                                                    Padding="12,9"
                                                    Background="#FF24282E"
                                                    CornerRadius="4">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="16" />
                                                        <ColumnDefinition Width="*" />
                                                    </Grid.ColumnDefinitions>
                                                    <Path x:Name="Arrow"
                                                          Width="6" Height="10"
                                                          HorizontalAlignment="Left"
                                                          VerticalAlignment="Center"
                                                          Data="M 1,1 L 5,5 L 1,9"
                                                          Fill="Transparent"
                                                          Stroke="#FFB8BDC5"
                                                          StrokeThickness="1.4"
                                                          StrokeStartLineCap="Round"
                                                          StrokeEndLineCap="Round"
                                                          StrokeLineJoin="Round"
                                                          RenderTransformOrigin="0.5,0.5" />
                                                    <ContentPresenter Grid.Column="1"
                                                                      Content="{TemplateBinding Content}"
                                                                      VerticalAlignment="Center"
                                                                      TextElement.FontSize="13"
                                                                      TextElement.FontWeight="SemiBold" />
                                                </Grid>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsChecked" Value="True">
                                                    <Setter TargetName="Arrow" Property="RenderTransform">
                                                        <Setter.Value>
                                                            <RotateTransform Angle="90" />
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="HeaderBackground" Property="Background" Value="#FF2B3037" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <ContentPresenter x:Name="Body"
                                                  Grid.Row="1"
                                                  Margin="10,7,10,10"
                                                  ContentSource="Content"
                                                  Visibility="Collapsed" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="True">
                                <Setter TargetName="Body" Property="Visibility" Value="Visible" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MediaInfoItemExpander"
               TargetType="Expander"
               BasedOn="{StaticResource MediaInfoSectionExpander}">
            <Setter Property="Margin" Value="0,3,0,3" />
        </Style>
    </Window.Resources>

    <Grid Background="Transparent">
        <Border Background="#FF1D2025"
                BorderBrush="#FF3C424B"
                BorderThickness="1"
                CornerRadius="12"
                ClipToBounds="True">
            <Grid ClipToBounds="True">
                <Grid.RowDefinitions>
                    <RowDefinition Height="42" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Border x:Name="DialogTitleBar" Background="#FF24272B"
                        CornerRadius="11,11,0,0">
                    <Grid Background="Transparent">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="42" />
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0"
                                Padding="17,0,8,0"
                                Background="Transparent"
                                MouseLeftButtonDown="TitleBar_OnMouseLeftButtonDown">
                            <TextBlock x:Name="DialogHeadingText"
                                       Text="Informations sur le média"
                                       IsHitTestVisible="False"
                                       Foreground="#FFF4F5F7"
                                       FontSize="18"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Center" />
                        </Border>
                        <Button Grid.Column="1" Style="{StaticResource DialogCloseWindowButton}"
                                Content="&#xE8BB;" Width="42" Height="42"
                                HorizontalAlignment="Right" VerticalAlignment="Top"
                                ToolTip="Fermer" Click="Close_OnClick" />
                    </Grid>
                </Border>

                <TextBlock x:Name="MediaTitleText" Grid.Row="1"
                           Margin="22,12,22,12"
                           Foreground="#FFA7ADB6"
                           FontSize="12"
                           TextTrimming="CharacterEllipsis" />

                <Border Grid.Row="2"
                        Margin="22,0"
                        Padding="8"
                        Background="#FF111419"
                        BorderBrush="#FF3A4049"
                        BorderThickness="1"
                        CornerRadius="4">
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <StackPanel x:Name="InformationSectionsPanel" />
                    </ScrollViewer>
                </Border>

                <Button Grid.Row="3" Content="Fermer"
                        Style="{StaticResource TextButton}"
                        Width="92" Margin="0,16,22,20"
                        HorizontalAlignment="Right"
                        Click="Close_OnClick" />
            </Grid>
        </Border>

        <!-- Zones invisibles de redimensionnement : quatre côtés et quatre coins. -->
    </Grid>
</Window>
